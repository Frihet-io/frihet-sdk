{
  "private": true,
  "scripts": {
    "build": "pnpm -r build",
    "clean": "pnpm -r clean",
    "typecheck": "pnpm -r typecheck",
    "check:publish-drift": "node scripts/check-publish-drift.mjs"
  },
  "engines": {
    "node": ">=18"
  }
}
